apiVersion: v1
kind: Service
metadata:
  name: {{ include "bee-overlay-exporter.fullname" . }}
  labels:
    {{- include "bee-overlay-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "bee-overlay-exporter.selectorLabels" . | nindent 4 }}
